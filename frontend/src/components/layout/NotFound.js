import React from 'react'
import { Grid, Typography } from '@material-ui/core'

const NotFound = () => {
  return (
    <Grid container justify="center">
      <Typography variant="h4">Xin lỗi, trang này không tồn tại!</Typography>
    </Grid>
  )
}

export default NotFound
